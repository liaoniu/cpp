#include <iostream>
#include <string>
#include <vector>


using std::vector, std::cin, std::cout;

int main()
{
    int i;
    vector<int> ivec;
    while (cin >> i)
        ivec.push_back(i);

    (ivec);
    return 0;
    

}